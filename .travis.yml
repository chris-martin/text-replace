language: nix

env:
  global:
    - LC_ALL=en_US.UTF-8
  matrix:
    - ATTR=ghc-8-0-2
    - ATTR=ghc-8-2-1

cache:
  directories:
    - /nix/store

script:
  - cd nix; nix-build --attr "$ATTR"

notifications:
  email: false
